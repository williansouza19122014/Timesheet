# Base URL of the backend (Render) for production/staging.
# Update with the public domain provided by Render when needed.
# Example: VITE_API_BASE_URL=https://timesheet-t0az.onrender.com
# In local development, if left empty the frontend auto-detects:
#   - If running on http://localhost:5173 (or 4173) it uses http://localhost:3000
#   - Otherwise, it reuses the app origin
VITE_API_BASE_URL=https://timesheet-t0az.onrender.com
