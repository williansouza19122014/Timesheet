# URL base do backend em produção.
# Exemplo: VITE_API_BASE_URL=https://minha-api.com
# Se deixar vazio, o frontend usará automaticamente a mesma origem da página.
VITE_API_BASE_URL=

# URL do backend em desenvolvimento para o proxy do Vite.
# Mantém o padrão http://localhost:4000 caso não seja definido.
VITE_DEV_SERVER_URL=http://localhost:4000
